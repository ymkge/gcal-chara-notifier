# Google OAuth 2.0 Credentials
# Obtain these from the Google Cloud Console: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET

# This must match one of the "Authorized redirect URIs" for your OAuth 2.0 client ID.
# For local development, this is typically the callback endpoint on your backend server.
GOOGLE_REDIRECT_URI=http://localhost:3001/api/auth/google/callback

# A secret key for encrypting sensitive data like refresh tokens.
# It MUST be a 32-byte (64-character hex) string.
# You can generate one with this Node.js command:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=YOUR_64_CHARACTER_HEX_ENCRYPTION_KEY

# Port for the backend server
PORT=3001

# Firebase Admin SDK Credentials
# Obtain these from your Firebase project settings -> Service accounts
# FIREBASE_PRIVATE_KEY: Ensure to replace actual newlines with '\n' if copying directly from JSON
FIREBASE_PROJECT_ID=YOUR_FIREBASE_PROJECT_ID
FIREBASE_PRIVATE_KEY=YOUR_FIREBASE_PRIVATE_KEY
FIREBASE_CLIENT_EMAIL=YOUR_FIREBASE_CLIENT_EMAIL

